{
  "name": "rqi-web-snippets",
  "version": "1.0.0",
  "main": "common.js",
  "scripts": {
    "test": "playwright test",
    "test:default": "playwright test --config=playwright.config.default.js",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "screenshots": "node scripts/generate-screenshots.js",
    "screenshots:clean": "del /q apps\\*.jpeg 2>nul || true"
  },
  "keywords": [],
  "author": "Rui Quintino",
  "license": "MIT",
  "description": "A collection of standalone web utilities and applications",
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
